
  # Redesign Service Desk Flow

  This is a code bundle for Redesign Service Desk Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  